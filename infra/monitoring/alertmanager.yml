route:
  receiver: default
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 2h

receivers:
  - name: default
    webhook_configs:
      - url: "http://backend:2000/api/health"
        send_resolved: true
